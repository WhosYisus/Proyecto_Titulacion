import 'package:flutter/material.dart';

class AppColors {
  // Colores principales
  static const primary = Color(0xFF6750A4);
  static const secondary = Color(0xFF625B71);
  static const tertiary = Color(0xFF7D5260);

  // Colores de fondo
  static const backgroundLight = Color(0xFFF6F5F7);
  static const backgroundDark = Color(0xFF1C1B1F);

  // Colores de superficie
  static const surfaceLight = Colors.white;
  static const surfaceDark = Color(0xFF2B2930);

  // Colores de texto
  static const textPrimaryLight = Color(0xFF1C1B1F);
  static const textSecondaryLight = Color(0xFF49454F);
  static const textPrimaryDark = Color(0xFFE6E1E5);
  static const textSecondaryDark = Color(0xFFCAC4D0);

  // Colores de acento
  static const accent = Color(0xFFE8DEF8);
  static const error = Color(0xFFB3261E);
  static const success = Color(0xFF146110);

  // Colores para el botón de grabación
  static const recordingStart = Color(0xFF6750A4);
  static const recordingActive = Color(0xFFE53935);
}

